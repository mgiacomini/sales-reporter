<%#
# Index

This view is the template for the index page.
It is responsible for rendering the search bar, header and pagination.
It renders the `_table` partial to display details about the resources.

## Local variables:

- `page`:
  An instance of [Administrate::Page::Collection][1].
  Contains helper methods to help display a table,
  and knows which attributes should be displayed in the resource's table.
- `resources`:
  An instance of `ActiveRecord::Relation` containing the resources
  that match the user's search criteria.
  By default, these resources are passed to the table partial to be displayed.
- `search_term`:
  A string containing the term the user has searched for, if any.

[1]: http://www.rubydoc.info/gems/administrate/Administrate/Page/Collection
%>

<% content_for(:title) do %>
  <%= display_resource_name(page.resource_name) %>
<% end %>

<header class="header">
  <h1 class="header__heading" id="page-title"><%= content_for(:title) %></h1>
  <div class="header__actions">
  </div>
</header>

<div style="margin-left: 20px;">
  <h2>General sales report of lasts 30 days</h2>
  <%= line_chart @orders.group_by_day(:wordpress_created_at).sum(:total) %>

  <h3>Sales by status (number of orders)</h3>
  <%= pie_chart Order.last_30_days.group(:status).count %>

  <h3>Sales by status ($$)</h3>
  <%= pie_chart Order.last_30_days.group(:status).sum(:total) %>


  <% Wordpress.all.each do |wordpress| %>
      <h3><%= wordpress.name %></h3>
      <%= line_chart wordpress.orders.last_30_days.converted.group_by_day(:wordpress_created_at).sum(:total) %>
  <% end %>
</div>